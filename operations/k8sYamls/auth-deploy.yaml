apiVersion: apps/v1
kind: Deployment
metadata:
  name: auth-deploy
  namespace: ingress-nginx
spec:
  replicas: 1
  selector:
    matchLabels:
      app: auth-app
  template:
    metadata:
      labels:
        app: auth-app
    spec:
      containers:
        - name: auth
          image: rentit/auth
          imagePullPolicy: Never
---
apiVersion: v1
kind: Service
metadata:
  name: auth-service
spec:
  selector:
    app: auth-app
  ports:
    - name: auth-port
      protocol: TCP
      port: 3000
      targetPort: 3000

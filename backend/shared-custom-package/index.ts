interface FruitCount {
  apple: number;
  orange: number;
  grapes: number;
  cherry: number;
}

const bucket1Count: FruitCount = {
  apple: 120,
  orange: 90,
  grapes: 20,
  cherry: 90,
};

console.log(bucket1Count);

export default bucket1Count;